# binaries
/portolan
/dist/

# local snapshots and rendered maps (root only — pkg/render/map.html is the
# embedded UI template and must be tracked)
*.snapshot.json
/snapshot.json
/map.html

# editor/OS noise
.idea/
.vscode/
*.swp
.DS_Store

# local review mockups / scratch renders
tmp/
